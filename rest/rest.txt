pizza={
    id,
    name,
    description,
    url,
    price->getter(sum(cost.ingredients) * 1.2 )
    ingredients:[{id,name,cost}]
}
ingredients={
    id,
    name,
    cost
}

1. dominio->la logica de negocio de la applicacion
3. servicios->
        validar la entrada de usuario
        gestionar logger
        gestionar transacciones en bb.dd
        mappear->crear la entidad del dominio
                 dominio->mapear a la salida
2. infraestructura
    persistencia en bb.dd o ficheros

4. controlador->HTTP->
        crear
        modificarlo
        delete
        recuperar por id
        recuperar por una query

                 ----dominio----
constrolador->servicio->infraestrctura->BB.DD

dominio
ingraestructura
servicio
controlador
